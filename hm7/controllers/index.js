const main = require('./main');
const news = require('./news');
const task = require('./task');
const auth = require('./auth');
const api = require('./api');

module.exports = {
    main, news, task, auth, api
};